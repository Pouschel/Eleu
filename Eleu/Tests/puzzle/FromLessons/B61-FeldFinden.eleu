var p = "
:def
w=Wall
c=Cat
g=Green Square
:grid
. 
c   g .

:meta
win=CatAt 4 1
name=B61 - Feld finden
funcs=move turn color 
:info
Bewege die Katze zum grünen Feld. 

Das grüne Feld befindet in jedem Testfall an einer
anderen Position.

Das Programm muss für alle Testfälle funktionieren.
:grid
.
c         g .
.
:meta
win=CatAt 10 1
";

fun solve(idx)
{
	_puzzle(p, idx);
	
	repeat(1000)
	{
		if (color()=="Green")
		{
		  break;
		}
		else move();
	}
	assert _isSolved();
}

solve(0);
solve(1);
